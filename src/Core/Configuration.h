#pragma once
// Global configuration settings for the customizer
namespace Config {
    // Player enhancement options
    bool bEnableEnhancedResilience = false;
    bool bEnableOptimizedAmmunition = false;
    float fMovementMultiplier = 1.0f;

    // Vehicle management options
    bool bEnableVehicleDurability = false;
    int iSelectedVehicleModel = 0; // Index for vehicle access

    // Visual preferences
    bool bShowOverlayInterface = true;
}
